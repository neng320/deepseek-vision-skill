[project]
name = "mimo-vision-mcp"
version = "0.1.0"
description = "MCP server exposing the mimo-vision skill's vision pipeline (multi-provider failover) as typed tools"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "pillow>=10.0",
]

[tool.uv]
package = false
